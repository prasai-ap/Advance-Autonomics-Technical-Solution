# Contributor guide

Use Python 3.11 and `uv`. Keep routes thin, database access in repositories, business
rules in services, and authorization in reusable policies. Run Ruff and pytest before commits.
Never commit `.env`, credentials, caches, virtual environments, or local databases.
